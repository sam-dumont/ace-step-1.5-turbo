apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: acestep-output
  namespace: acestep
spec:
  accessModes:
    - ReadWriteOnce
  # Set your storage class here, or remove to use the cluster default
  # storageClassName: standard
  resources:
    requests:
      storage: 20Gi
